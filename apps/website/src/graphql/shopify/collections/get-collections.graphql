query GetCollections {
  collections(first: 50) {
    nodes {
      id
      handle
      title
      description
      image {
        id
        url
        altText
        width
        height
      }
    }
  }
}
